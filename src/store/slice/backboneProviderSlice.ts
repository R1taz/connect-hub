import { createSlice } from '@reduxjs/toolkit'

interface State {}

const initialState: State = {}

const backboneProviderSlice = createSlice({
	name: 'backboneProviderSlice',
	initialState,
	reducers: {},
})

export default backboneProviderSlice.reducer
