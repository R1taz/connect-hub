import { Button, styled } from '@mui/material'

export const CustomButton = styled(Button)(() => ({
	backgroundColor: 'black',
	color: 'white',
	padding: '15px 30px',
	borderRadius: '0px',
	width: '60%',
}))
